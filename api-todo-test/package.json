{
  "name": "api-todo-test",
  "version": "1.0.0",
  "description": "Playwright e2e tests for API Todo",
  "scripts": {
    "test": "npx playwright test",
    "pretest": "npm run prestart && npx playwright install chromium",
    "test:playwright": "npx playwright test",
    "build": "tsc",
    "prestart": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "playwright": "^1.39.0",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.39.0",
    "@types/node": "^20.8.9"
  }
}
